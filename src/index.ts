// twire is just a skeleton function that doesn't do anything, but to make the
// code compile, and to mark init functions for dependency injections.
export function tswire(_providers: any[]): void {
  return;
}
